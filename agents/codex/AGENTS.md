## Codex CLI
- rtk usage rules are appended below by `rtk init`; follow them for every shell command.
- No subagents here: work sequentially, keep each change small and verified before moving on.
- Edit via apply_patch; never echo whole files back.
